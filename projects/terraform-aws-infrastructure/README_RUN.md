# Terraform AWS Infra - Run Instructions

1. Set variables: `export TF_VAR_bucket_name=my-unique-bucket-12345`
2. Initialize: `terraform init`
3. Plan: `terraform plan`
4. Apply: `terraform apply`

Note: Configure AWS credentials via environment variables or shared credentials file.
