# Kubernetes Run Instructions

Apply manifests:

```
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
```

Check pods and service:

```
kubectl get pods
kubectl get svc sample-service
```
