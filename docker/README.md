# Docker

Containers make deployments predictable. These notes introduce Docker and a couple of developer workflows.

How to use this folder
- Start with `docker-basics.md` and run the Dockerized app in `/projects`.
- Use `docker-compose.md` to orchestrate multiple services locally.

Pro tip: Keep images small and rebuild often to stay comfortable with Docker caching behavior.
